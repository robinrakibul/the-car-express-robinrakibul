import React from 'react';
import './Product.css';

const Product = (props) => {
    const { title, price, image } = props;
    return (
        <div>
            <h3>Cars</h3>

        </div>
    );
};

export default Product;